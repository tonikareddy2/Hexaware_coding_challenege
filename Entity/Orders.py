class Orders:
    def __init__(self, orderId, userId, productId, orderDate):
        self.orderId = orderId
        self.userId = userId
        self.productId = productId
        self.orderDate = orderDate
